#include <iostream>
#include "MyList.h"
using namespace std;

namespace samples
{
    template<typename T>
    MyList<T>::MyList()
    {
    }

    template<typename T>
    int MyList<T>::GetSize()
    {
        return 0;
    }
}